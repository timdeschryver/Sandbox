{
	"name": "sandbox.e2e",
	"version": "0.0.0",
	"scripts": {
		"format": "oxfmt .",
		"format:check": "oxfmt --list-different .",
		"lint": "oxlint --deny-warnings --format=agent .",
		"lint:fix": "oxlint --deny-warnings --format=agent --fix .",
		"test": "playwright test",
		"test:ui": "playwright test --ui"
	},
	"type": "module",
	"devDependencies": {
		"@azure/identity": "catalog:",
		"@azure/playwright": "catalog:",
		"@playwright/test": "catalog:playwright",
		"@types/node": "catalog:",
		"dotenv": "catalog:",
		"typescript": "catalog:"
	}
}
